import { IsInstrumentDirective } from './is-instrument.directive';

describe('IsInstrumentDirective', () => {
  it('should create an instance', () => {
    const directive = new IsInstrumentDirective();
    expect(directive).toBeTruthy();
  });
});
