import { IsUserDirective } from './is-user.directive';

describe('IsUserDirective', () => {
  it('should create an instance', () => {
    const directive = new IsUserDirective();
    expect(directive).toBeTruthy();
  });
});
